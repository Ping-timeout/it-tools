import{A as e,E as t,F as n,O as r,Tt as i,a,lt as o,q as s,s as c,w as l}from"./vue.runtime.esm-bundler-C0qeGFLU.js";import{t as u}from"./use-theme-vars-D12EPShX.js";import{t as d}from"./_plugin-vue_export-helper-BDNMzG2s.js";n();var f={class:`markdown-body`},p={__name:`docker`,setup(e,{expose:n}){return n({frontmatter:{}}),(e,n)=>(s(),l(`div`,f,[...n[0]||=[t(`<p>Docker provides the ability to package and run an application in a loosely isolated environment called a container. The isolation and security allows you to run many containers simultaneously on a given host. Containers are lightweight and contain everything needed to run the application, so you do not need to rely on what is currently installed on the host. You can easily share containers while you work, and be sure that everyone you share with gets the same container that works in the same way.</p><h2>Installation &amp; Resources</h2><ul><li>Docker Desktop (Mac, Linux, Windows): <a href="https://docs.docker.com/desktop">https://docs.docker.com/desktop</a></li><li>Example projects using Docker: <a href="https://github.com/docker/awesome-compose">https://github.com/docker/awesome-compose</a></li><li>Official documentation: <a href="https://docs.docker.com">https://docs.docker.com</a></li></ul><h2>General Commands</h2><p>Start the Docker daemon:</p><pre><code class="language-bash">dockerd
</code></pre><p>Get help with Docker (works on all subcommands, e.g. <code>docker run --help</code>):</p><pre><code class="language-bash">docker --help
</code></pre><p>Display system-wide information:</p><pre><code class="language-bash">docker info
</code></pre><p>Show Docker version (client and server):</p><pre><code class="language-bash">docker version
</code></pre><h2>Images</h2><p>Docker images are a lightweight, standalone, executable package of software that includes everything needed to run an application: code, runtime, system tools, system libraries, and settings.</p><p>Build an image from a Dockerfile in the current directory:</p><pre><code class="language-bash">docker build -t &lt;image_name&gt; .
</code></pre><p>Build an image without using the cache:</p><pre><code class="language-bash">docker build -t &lt;image_name&gt; . --no-cache
</code></pre><p>Build with a specific tag (e.g. <code>myapp:v1.2</code>):</p><pre><code class="language-bash">docker build -t &lt;image_name&gt;:&lt;tag&gt; .
</code></pre><p>List local images:</p><pre><code class="language-bash">docker images
</code></pre><p>Tag an image for pushing to a registry:</p><pre><code class="language-bash">docker tag &lt;image&gt; &lt;username&gt;/&lt;image&gt;:&lt;tag&gt;
</code></pre><p>Delete an image:</p><pre><code class="language-bash">docker rmi &lt;image_name&gt;
</code></pre><p>Remove dangling (unused) images:</p><pre><code class="language-bash">docker image prune
</code></pre><p>Remove all unused images, not just dangling:</p><pre><code class="language-bash">docker image prune -a
</code></pre><p>Show the layers of an image:</p><pre><code class="language-bash">docker history &lt;image_name&gt;
</code></pre><h2>Docker Hub</h2><p>Docker Hub is a service for finding and sharing container images. Learn more at <a href="https://hub.docker.com">https://hub.docker.com</a></p><p>Log in to Docker Hub:</p><pre><code class="language-bash">docker login -u &lt;username&gt;
</code></pre><p>Log out:</p><pre><code class="language-bash">docker logout
</code></pre><p>Search Docker Hub for an image:</p><pre><code class="language-bash">docker search &lt;image_name&gt;
</code></pre><p>Pull an image from Docker Hub:</p><pre><code class="language-bash">docker pull &lt;image_name&gt;
</code></pre><p>Pull a specific tagged version:</p><pre><code class="language-bash">docker pull &lt;image_name&gt;:&lt;tag&gt;
</code></pre><p>Publish an image to Docker Hub:</p><pre><code class="language-bash">docker push &lt;username&gt;/&lt;image_name&gt;
</code></pre><h2>Containers</h2><p>A container is a runtime instance of a Docker image. A container will always run the same, regardless of the infrastructure. Containers isolate software from its environment and ensure it works uniformly across development, staging, and production.</p><h3>Running containers</h3><p>Create and run a container from an image:</p><pre><code class="language-bash">docker run &lt;image_name&gt;
</code></pre><p>Run with a custom name:</p><pre><code class="language-bash">docker run --name &lt;container_name&gt; &lt;image_name&gt;
</code></pre><p>Run in the background (detached):</p><pre><code class="language-bash">docker run -d &lt;image_name&gt;
</code></pre><p>Run interactively with a shell:</p><pre><code class="language-bash">docker run -it &lt;image_name&gt; bash
</code></pre><p>Auto-remove the container when it exits:</p><pre><code class="language-bash">docker run --rm &lt;image_name&gt;
</code></pre><p>Publish a container’s port to the host:</p><pre><code class="language-bash">docker run -p &lt;host_port&gt;:&lt;container_port&gt; &lt;image_name&gt;
</code></pre><p>Set an environment variable:</p><pre><code class="language-bash">docker run -e KEY=value &lt;image_name&gt;
</code></pre><p>Load environment variables from a file:</p><pre><code class="language-bash">docker run --env-file ./env.list &lt;image_name&gt;
</code></pre><p>Mount a named volume:</p><pre><code class="language-bash">docker run -v &lt;volume_name&gt;:/path/in/container &lt;image_name&gt;
</code></pre><p>Bind-mount the current directory:</p><pre><code class="language-bash">docker run -v $(pwd):/app &lt;image_name&gt;
</code></pre><p>Set a restart policy (options: <code>no</code>, <code>on-failure</code>, <code>always</code>, <code>unless-stopped</code>):</p><pre><code class="language-bash">docker run --restart unless-stopped &lt;image_name&gt;
</code></pre><p>Attach to a specific network:</p><pre><code class="language-bash">docker run --network &lt;network_name&gt; &lt;image_name&gt;
</code></pre><h3>Managing containers</h3><p>List currently running containers:</p><pre><code class="language-bash">docker ps
</code></pre><p>List all containers (running and stopped):</p><pre><code class="language-bash">docker ps -a
</code></pre><p>Start a stopped container:</p><pre><code class="language-bash">docker start &lt;container_name&gt;
</code></pre><p>Stop a running container:</p><pre><code class="language-bash">docker stop &lt;container_name&gt;
</code></pre><p>Restart a container:</p><pre><code class="language-bash">docker restart &lt;container_name&gt;
</code></pre><p>Remove a stopped container:</p><pre><code class="language-bash">docker rm &lt;container_name&gt;
</code></pre><p>Force-remove a running container:</p><pre><code class="language-bash">docker rm -f &lt;container_name&gt;
</code></pre><p>Remove all stopped containers:</p><pre><code class="language-bash">docker container prune
</code></pre><p>Rename a container:</p><pre><code class="language-bash">docker rename &lt;old_name&gt; &lt;new_name&gt;
</code></pre><h3>Inspecting and interacting</h3><p>Open a shell inside a running container:</p><pre><code class="language-bash">docker exec -it &lt;container_name&gt; bash
</code></pre><p>Use <code>sh</code> for minimal images (e.g. alpine) that don’t ship bash:</p><pre><code class="language-bash">docker exec -it &lt;container_name&gt; sh
</code></pre><p>Fetch logs:</p><pre><code class="language-bash">docker logs &lt;container_name&gt;
</code></pre><p>Follow logs (like <code>tail -f</code>):</p><pre><code class="language-bash">docker logs -f &lt;container_name&gt;
</code></pre><p>Show only the last 100 lines:</p><pre><code class="language-bash">docker logs --tail 100 &lt;container_name&gt;
</code></pre><p>Show low-level container details (JSON):</p><pre><code class="language-bash">docker inspect &lt;container_name&gt;
</code></pre><p>Live resource usage stats for all containers:</p><pre><code class="language-bash">docker stats
</code></pre><p>Show running processes inside a container:</p><pre><code class="language-bash">docker top &lt;container_name&gt;
</code></pre><p>Copy a file out of a container:</p><pre><code class="language-bash">docker cp &lt;container_name&gt;:/path/in/container ./
</code></pre><p>Copy a file into a container:</p><pre><code class="language-bash">docker cp ./local_file &lt;container_name&gt;:/path/
</code></pre><p>Show filesystem changes since container start:</p><pre><code class="language-bash">docker diff &lt;container_name&gt;
</code></pre><h2>Volumes</h2><p>Volumes persist data outside the container’s writable layer, so data survives container removal.</p><p>Create a named volume:</p><pre><code class="language-bash">docker volume create &lt;volume_name&gt;
</code></pre><p>List volumes:</p><pre><code class="language-bash">docker volume ls
</code></pre><p>Show details about a volume:</p><pre><code class="language-bash">docker volume inspect &lt;volume_name&gt;
</code></pre><p>Remove a volume:</p><pre><code class="language-bash">docker volume rm &lt;volume_name&gt;
</code></pre><p>Remove all unused volumes:</p><pre><code class="language-bash">docker volume prune
</code></pre><p>Attach a named volume to a container (managed by Docker):</p><pre><code class="language-bash">docker run -v &lt;volume_name&gt;:/data &lt;image_name&gt;
</code></pre><p>Bind-mount a host path into a container:</p><pre><code class="language-bash">docker run -v $(pwd):/app &lt;image_name&gt;
</code></pre><p>Equivalent bind mount using the <code>--mount</code> syntax:</p><pre><code class="language-bash">docker run --mount type=bind,src=$(pwd),dst=/app &lt;image_name&gt;
</code></pre><h2>Networks</h2><p>Containers on the same network can reach each other by container name.</p><p>List networks:</p><pre><code class="language-bash">docker network ls
</code></pre><p>Create a network:</p><pre><code class="language-bash">docker network create &lt;network_name&gt;
</code></pre><p>Show network details:</p><pre><code class="language-bash">docker network inspect &lt;network_name&gt;
</code></pre><p>Attach a running container to a network:</p><pre><code class="language-bash">docker network connect &lt;network_name&gt; &lt;container_name&gt;
</code></pre><p>Detach a container from a network:</p><pre><code class="language-bash">docker network disconnect &lt;network_name&gt; &lt;container_name&gt;
</code></pre><p>Remove a network:</p><pre><code class="language-bash">docker network rm &lt;network_name&gt;
</code></pre><p>Remove all unused networks:</p><pre><code class="language-bash">docker network prune
</code></pre><h2>Docker Compose</h2><p>Compose runs multi-container apps from a single <code>compose.yaml</code> (or <code>docker-compose.yml</code>) file.</p><p>Start all services (foreground):</p><pre><code class="language-bash">docker compose up
</code></pre><p>Start all services (detached):</p><pre><code class="language-bash">docker compose up -d
</code></pre><p>Rebuild images before starting:</p><pre><code class="language-bash">docker compose up --build
</code></pre><p>Stop and remove containers and networks:</p><pre><code class="language-bash">docker compose down
</code></pre><p>Also remove named volumes:</p><pre><code class="language-bash">docker compose down -v
</code></pre><p>List services in the project:</p><pre><code class="language-bash">docker compose ps
</code></pre><p>Follow logs for all services:</p><pre><code class="language-bash">docker compose logs -f
</code></pre><p>Follow logs for one service:</p><pre><code class="language-bash">docker compose logs -f &lt;service&gt;
</code></pre><p>Build (or rebuild) services:</p><pre><code class="language-bash">docker compose build
</code></pre><p>Pull service images:</p><pre><code class="language-bash">docker compose pull
</code></pre><p>Open a shell in a running service:</p><pre><code class="language-bash">docker compose exec &lt;service&gt; bash
</code></pre><p>Run a one-off command in a new container:</p><pre><code class="language-bash">docker compose run --rm &lt;service&gt; &lt;command&gt;
</code></pre><p>Restart a single service:</p><pre><code class="language-bash">docker compose restart &lt;service&gt;
</code></pre><p>Validate and view the resolved compose file:</p><pre><code class="language-bash">docker compose config
</code></pre><h2>System &amp; Cleanup</h2><p>Disk usage adds up fast. These commands reclaim space.</p><p>Show disk usage (images, containers, volumes, build cache):</p><pre><code class="language-bash">docker system df
</code></pre><p>Remove stopped containers, dangling images, and unused networks:</p><pre><code class="language-bash">docker system prune
</code></pre><p>Also remove all unused images (not just dangling):</p><pre><code class="language-bash">docker system prune -a
</code></pre><p>Also remove unused volumes (destructive — double-check before running):</p><pre><code class="language-bash">docker system prune -a --volumes
</code></pre><p>Clean the build cache:</p><pre><code class="language-bash">docker builder prune
</code></pre><h2>Quick Reference: Common Flags</h2><table><thead><tr><th>Flag</th><th>Meaning</th></tr></thead><tbody><tr><td><code>-d</code></td><td>Detached (run in background)</td></tr><tr><td><code>-it</code></td><td>Interactive + TTY (for shells)</td></tr><tr><td><code>--rm</code></td><td>Remove container when it exits</td></tr><tr><td><code>-p host:container</code></td><td>Publish a port</td></tr><tr><td><code>-v src:dst</code></td><td>Mount volume or bind mount</td></tr><tr><td><code>-e KEY=value</code></td><td>Environment variable</td></tr><tr><td><code>--name</code></td><td>Assign a container name</td></tr><tr><td><code>--network</code></td><td>Attach to a network</td></tr><tr><td><code>--restart</code></td><td>Restart policy (<code>no</code>, <code>on-failure</code>, <code>always</code>, <code>unless-stopped</code>)</td></tr></tbody></table>`,193)]]))}};a(),n(),o();var m=d(e({__name:`docker-memo`,setup(e){c(e=>({c4cf6bc0:i(t).cardColor,v5d0dfa6e:i(t).textColor1}));let t=u();return(e,t)=>(s(),l(`div`,null,[r(i(p),{style:{"overflow-x":`auto`}})]))}}),[[`__scopeId`,`data-v-18599112`]]);export{m as default};